# Example only — put real keys in via `kubectl create secret` or a sealed secret.
apiVersion: v1
kind: Secret
metadata:
  name: provider-keys
type: Opaque
stringData:
  ANTHROPIC_API_KEY: ""
  OPENAI_API_KEY: ""
  INGEST_KEY: dev-ingest-key
